# BomberDOM
A multiplayer version of the classic Bomberman game, built with a custom mini-framework and WebSockets for real-time gameplay.

# thought process:

Create the basic server structure first:

Basic Express Server Setup

Create server.js with a basic Express server
Set up the WebSocket server using the 'ws' library
Serve static files from your client directory

## Issues:

"ws": "^8.13.0"

_____________
HTML Structure

Create login, waiting, and game screens
Set up the UI elements for chat and game info


Connect to Server

Establish WebSocket connection
Implement message sending/receiving logic
